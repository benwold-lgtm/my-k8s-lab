apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui
  namespace: ai-stack
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: "manual"
  volumeName: open-webui-nfs-pv  # This forces the exact match to your PV