replicaCount: 1

image:
  repository: vllm/vllm-openai
  tag: latest # Or a specific version like v0.7.0
  pullPolicy: Always

model:
  name: "mistralai/Mistral-Nemo-Instruct-2407" # Change this to swap models
  downloadDir: "/data"

resources:
  limits:
    nvidia.com/gpu: 1
  requests:
    cpu: 2
    memory: 16Gi

storage:
  className: local-path # Using your fast 2TB NVMe!
  size: 50Gi